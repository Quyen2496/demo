-- Bước 1: Tạo cơ sở dữ liệu mới tên là demo
CREATE DATABASE demo;

-- Bước 2: Chọn cơ sở dữ liệu demo để sử dụng
USE demo;

-- Bước 3: Tạo bảng Student với các thuộc tính theo yêu cầu
CREATE TABLE Student (
    id INT,
    name VARCHAR(200),
    age INT,
    country VARCHAR(50)
);
